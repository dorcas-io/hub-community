<div class="card-panel hoverable">
    <p class="{{ $paragraphClass or 'flow-text' }}">
        {{ $slot }} Use the <i class="material-icons">{{ $icon or 'assistant' }}</i>
        button to add one now.
    </p>
</div>